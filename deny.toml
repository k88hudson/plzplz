# cargo-deny config, scoped to supply-chain checks (advisories, bans, sources).
# Run locally with: cargo deny check advisories bans sources

[advisories]
version = 2
yanked = "deny"

[bans]
multiple-versions = "warn"

[sources]
unknown-registry = "deny"
unknown-git = "deny"
